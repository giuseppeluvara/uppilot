from django.apps import AppConfig


class ConoscenzaConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.conoscenza"
    verbose_name = "Grafo della conoscenza"
